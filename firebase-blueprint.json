import { Type } from "@google/genai";

export const firebaseBlueprint = {
  entities: {
    ContactSubmission: {
      title: "Contact Submission",
      description: "A message sent via the contact form on the Cosmivon website.",
      type: "object",
      properties: {
        name: {
          type: "string",
          description: "The name of the sender."
        },
        email: {
          type: "string",
          format: "email",
          description: "The email address of the sender."
        },
        company: {
          type: "string",
          description: "The company of the sender (optional)."
        },
        message: {
          type: "string",
          description: "The message content."
        },
        createdAt: {
          type: "string",
          format: "date-time",
          description: "The timestamp when the message was submitted."
        }
      },
      required: ["name", "email", "message", "createdAt"]
    }
  },
  firestore: {
    "/contacts/{contactId}": {
      schema: "ContactSubmission",
      description: "Collection of contact form submissions."
    }
  }
};
